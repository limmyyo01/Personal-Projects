#include <stdio.h>
int main (void)
{
    string first = get_string ( "what is your first name ?");
    string second = get_string ("what is your second name ?");
    printf("hello, %s %s\n" first second ");

}